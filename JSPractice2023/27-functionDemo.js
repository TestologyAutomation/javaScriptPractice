// Function to compute the product of p1 and p2
// p1 and p2 is formal parameters 
function myFunction(p1, p2) {
    return p1 * p2;
  }

  let w = myFunction(3,70); // function call
  // 3 and 70 is actual parameters
  console.log("w="+ w);

  let c = 30, d= 40;
  w = myFunction(c,d); // function call
  // c and d is actual parameters
  console.log("w="+ w);

  console.log(Math.pow(6,3));



function square(a)
{
    return a*a;
}

console.log(square(5));













  
  


